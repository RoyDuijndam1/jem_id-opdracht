type Product = {
    name: string
    description: string
    price: number
    height: number
    diameter: number
    standingPlace: string
    wateringNeeds: string
    photoUrl: string
}

export default Product;
